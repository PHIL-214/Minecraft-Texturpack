execute if score hunter_count arena > arena_count arena run kill @e[tag=hunter, sort=random, limit=1, distance=..100]
execute if score victim_count arena > arena_count arena run kill @e[tag=victim, sort=random, limit=1, distance=..100]
